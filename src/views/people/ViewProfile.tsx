/**
 * Re-export from feature for backwards compatibility.
 * Prefer importing from "../../features/profile" in new code.
 */
export {
  ViewProfile,
  ProfileSkeleton,
  ProfileBannerArea,
  ProfileStoreView,
  ProfileServicesView,
  LightBannerContainer,
  DarkBannerContainer,
  external_resource_regex,
} from "../../features/profile"
