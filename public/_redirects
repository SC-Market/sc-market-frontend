# Cloudflare Pages SPA routing
# Sitemap routes are proxied to the API via functions/_middleware.ts

# Redirect all routes to index.html for client-side routing.
# NOTE: CF Pages applies an implicit SPA fallback that runs BEFORE _redirects, so
# an `/assets/* -> 404` rule here has NO effect (a missing asset still returns
# 200 index.html). Stale-chunk safety is therefore handled client-side: the SW no
# longer caches assets (src/sw.ts), and lazy-route load failures trigger a
# rate-limited reload (RouteErrorFallback + index.html inline guard + assetReloadGuard).
/*    /index.html   200
